/**
 * @deprecated Use Category entity instead of SizeType enum.
 * Sizes now use child categories for better flexibility.
 */
export enum SizeType {
  TOP = 'top',
  BOTTOM = 'bottom',
  ACCESSORY = 'accessory',
}
